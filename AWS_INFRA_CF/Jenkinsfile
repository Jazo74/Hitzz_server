pipeline {
    agent any
    
    stages {
        stage("building_aws_infra") {
            steps {
                sh "aws cloudformation create-stack --stack-name webhitzz_infra --template-body file://var/jenkins_home/workspace/Webhitzz_infra/AWS_INFRCA_CF/soloEC2wSSMwUb2004.yaml --capabilities CAPABILITY_NAMED_IAM"
            }
        }
    }
}
